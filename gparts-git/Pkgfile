# Description: Component Management System
# URL:         http://git.geda-project.org
# Packager:    Joe9, joe9mail at gmail dot com
# Maintainer:  Ryan Mullen, rmmullen at gmail dot com
# Depends on:  gtk tk libgd

name=gparts-git
version=git
release=1
source=()

build() {
   cd $PKGMK_SOURCE_DIR

   if cd gparts ; then
       git pull
   else
       git clone git://git.geda-project.org/gparts.git gparts
   fi

   cd $PKGMK_SOURCE_DIR/gparts

   ./autogen.sh
   ./configure --prefix=/usr \
          --mandir=/usr/man \
          --disable-update-desktop-database \
          --disable-update-mime-database \
          --disable-dbus \
          --disable-gl \
          --disable-doc \
          --with-printer=""
   make
   make DESTDIR=$PKG install
   rm -r $PKG/usr/share/{doc,applications,icons,mime,mimelnk,locale}
}
